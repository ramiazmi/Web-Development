<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid" style="font-family: 'Droid Arabic Kufi'; font-size: 15px; background: #3c8dbc; color: #FFFFF;">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <a class="navbar-brand" href="/" style="background: #3c8dbc; color: white;">&nbsp;
              <i class="fa fa-home fa-lg"></i>&nbsp; نظام تقييم المقترحات
            </a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1"> 
            <ul class="nav navbar-nav">

                <li><a href="/users/sign_in" style = "color: white;font-size: 16px;"><span class="fa fa-sign-in"></span>&nbsp;تسجيل دخول</a></li>
                <% 
                active_grant_id=Grant.active_grant_id
                
                if (active_grant_id!=-1)  
                  grant=Grant.find(active_grant_id)
                  if (grant.proposal_period?)  %>  
                    <li><a href="#" style ="color: white;font-size: 16px;">|</a></li>
                    <li><a href="/users/sign_up" style = "color: white;font-size: 16px;"><span class="fa fa-file-text"></span>&nbsp;تسجيل</a></li>
                <% end
                 end %>
                <li>
                    <a href="#" style="color: white;"> <%= local_date(Date.current, '%B %e, %Y') %> </a>
                </li> 
            </ul>
            </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
</nav> 
<div class="container-fluid main-container" style="padding-top: 80px;" >
    <div class="col-md-12 content">
        <div class="panel panel-default">
            <div class="panel-body">    
                <% if notice %>
                              <div id="flash_notice" class="alert alert-success alert-dismissable">
                                <i class="fa fa-check"></i>
                                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                                <b><%= notice %></b>
                              </div>
                            <% end %>

                            <% if alert %>
                              <div id="flash_notice" class="alert alert-danger alert-dismissable">
                                <i class="fa fa-ban"></i>
                                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                                <b><%= alert %></b>
                              </div>
                            <% end %>
                <%= yield %>
            </div>
        </div>
    </div>
</div>    


